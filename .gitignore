node_modules
.next
out
dist
.env
.env.local
.env*.local
*.log
.DS_Store
.vercel
.turbo
.pnpm-store
prisma/dev.db
prisma/dev.db-journal
coverage

# Local Netlify folder (legacy — project migrated to Vercel)
.netlify

# Vercel
.vercel

# Local Claude/agent state
.claude/

# Local-only assets and artifacts (not for production)
image for web/
deno.lock
/norvex_sports_logo.png

# Runtime user uploads (served from /uploads, not source-controlled)
public/uploads/
